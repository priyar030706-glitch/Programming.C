
/* Write a C program to check whether a customer gets a discount */
#include <stdio.h>
int main() {
    float amount;
    printf("Enter purchase amount: ");
    scanf("%f", &amount);
    if (amount >= 1000)
        printf("Discount Available");
    else
        printf("No Discount");
    return 0;
}
