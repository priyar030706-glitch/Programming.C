/* Write a C program to find the average marks of a student */
#include <stdio.h>
int main() {
    float m1 = 80, m2 = 70, m3 = 90, avg;
    avg = (m1 + m2 + m3) / 3;
    printf("Average Marks = %.2f", avg);
    return 0;
}
