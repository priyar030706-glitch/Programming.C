#include <stdio.h>
#include <string.h>

int main() {
    char m[] = "9876543210";  // hardcoded number
    if(strlen(m)==10)
        printf("Valid mobile number");
    else
        printf("Invalid mobile number");
    return 0;
}
