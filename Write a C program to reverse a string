/* Write a C program to reverse a string */
#include <stdio.h>
#include <string.h>

int main() {
    char s[50];
    int i;

    scanf("%s", s);   // input string (no spaces)

    for (i = strlen(s) - 1; i >= 0; i--) {
        printf("%c", s[i]);   // correct usage of %c
    }

    return 0;
}
