/* Write a C program to convert temperature using function */
#include <stdio.h>

float celsiusToFahrenheit(float c) {
    return (c*9/5)+32;
}

int main() {
    float c;
    scanf("%f", &c);
    printf("Fahrenheit = %.2f", celsiusToFahrenheit(c));
    return 0;
}
